name: Run Bruno

on:
  workflow_dispatch:

jobs:
  bruno:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Bruno CLI
        run: npm install

      - name: Show versions
        run: |
          node -v
          bru --version

      - name: Run Bruno (sit01)
        working-directory: ./Bruno/Delphi
        run: bru:run