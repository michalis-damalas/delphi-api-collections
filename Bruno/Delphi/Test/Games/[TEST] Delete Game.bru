meta {
  name: [TEST] Delete Game
  type: http
  seq: 1
}

delete {
  url: {{delphi-game-management}}/api/test/games/{{test_recordId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authClientToken}}
}

settings {
  encodeUrl: true
}
