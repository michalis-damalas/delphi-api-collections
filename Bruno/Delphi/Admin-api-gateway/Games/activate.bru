meta {
  name: activate
  type: http
  seq: 7
}

put {
  url: {{delphi-admin-api-gateway}}/api/v1/games/{{test_recordId}}/activate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}
