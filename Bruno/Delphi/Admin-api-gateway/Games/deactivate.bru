meta {
  name: deactivate
  type: http
  seq: 11
}

put {
  url: {{delphi-admin-api-gateway}}/api/v1/games/{{test_recordId}}/deactivate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
}
