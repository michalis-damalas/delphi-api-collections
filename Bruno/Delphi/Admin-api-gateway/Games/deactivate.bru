meta {
  name: deactivate
  type: http
  seq: 11
}

put {
  url: {{delphi-admin-api-gateway}}/api/v1/games/{{test_recordId}}/deactivate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
}
